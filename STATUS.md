# Status

> **Last updated:** 2026-05-20 (v0.9.2 shipped; post-v0.9.1 self-audit closed two P1 cross-stack tracer gaps (TS `.parse()` + Python `.stream()`), aligned `.env` ignore in fallback FS walk, refreshed public READMEs to v0.9.x and dropped em dashes from llms.txt + landing-page copy).

## Where we are

**Agent-driven and human-driven install flows.** The wizard ships as both `gravel init` (interactive TTY walk for humans) and `gravel mount` / `gravel prompts` / `gravel traces` step subcommands with `--plan` (JSON) / `--apply` for installing agents (Claude Code, Codex). llms.txt narrates the step flow.

**Wedge backend is live and on PyPI + npm.** Customers can wizard-install on TS or Python hosts, sign in to a default-password dashboard (or auto-admin on loopback), list and edit manifest prompts (file-type or embedded with char-offset slicing), submit drafts as a single GitHub PR via the `gravel[bot]` GitHub App with the manifest atomically rewritten to keep subsequent prompts' offsets valid, view live traces, and leave feedback.

**Cross-stack parity is real.** Five integrations — FastAPI, ASGI, WSGI, Django, Flask — all delegate to one shared dispatcher (`python/gravel/src/artanis_gravel/_handler.py`) and return byte-equal responses for the same request. The TS handler is the second canon; CI cross-references both. Tracing auto-patches cover OpenAI / Anthropic / Gemini / LangChain on Python (Vercel AI + generic `fetch` on TS as well).

**Cloud is live.** Control plane at `https://gravel.artanis.ai` (Next.js 15 + Clerk + Drizzle on Vercel + Neon, prod Clerk instance). Routes: `/api/health`, `/api/judge`, `/api/analyze`, `/api/projects`, `/api/cli/auth/*`, `/api/cli/github/install/start`, `/api/cli/github/installation-token`, `/api/webhooks/clerk`, `/api/webhooks/polar`.

**Judge + eval runner shipped** with project ownership enforcement and audit logging. **Mallet `analyzePrompt()` shipped** with Clerk-org rate-limiting through the CP proxy.

## Recent release activity (May 2026)

- **v0.9.2** — Post-v0.9.1 self-audit (2026-05-20). No new dogfooding feedback; instead a full agent-driven sweep across cross-stack parity, money-path test coverage, customer-facing copy, and dead-code drift. Two P1 cross-stack tracer gaps closed: (a) **TS `Anthropic.Messages.prototype.parse` is now patched** alongside `.create()` and `.stream()` (Python had it from v0.9.1; TS was silently un-patched, so structured-output calls landed as generic `fetch:anthropic.messages` rows with no model / input richness). Pinned by a new `tests/tracing-anthropic-integration.test.ts` that exercises real `@anthropic-ai/sdk` against an HTTP mock and asserts exactly one `anthropic.messages.parse` row plus the lack of double-record when `.parse()` internally delegates to `.create()` (it does; we now wrap the original call in both `runWithSdkTracingDisabled` and `runWithFetchTracingDisabled` so the inner SDK-level recorder short-circuits). (b) **Python `Messages.stream` context-manager helper now traces** (TS had it from v0.8.x; Python was silently un-patched). Implementation does class-level patches on `MessageStreamManager.__enter__` + `__exit__` so the captured (started_at, model, input) propagates across the user's `with client.messages.stream(...) as stream: ...` block and records the consolidated row on exit via `stream.get_final_message()`. New `push_fetch_tracing_disabled` / `pop_fetch_tracing_disabled` singleton APIs let the suppression span the enter/exit pair (the underlying HTTP fires INSIDE the with block, after the SDK-level wrapper returns). Pinned by `test_messages_stream_records_exactly_one_row` in `test_tracing_anthropic_real.py`. **`.env` ignore-dir parity:** the FS-fallback ignore list in both `packages/sdk-ts/src/manifest/scan.ts` and `python/gravel/src/artanis_gravel/manifest/scan.py` now matches Go's by listing `.env` (previously the two SDKs would recurse into a `.env/` directory on non-git checkouts). **Docs:** all public-facing READMEs (root + sdk-ts + python/gravel) bumped to v0.9.x; CONTRIBUTING, UPGRADING, sdk-ts/README, plus `gravel/llms.txt` and `gravel/index.html` swept clean of em dashes per the copy rule (commas / colons / parens replace; ROADMAP.md + SOURCES.md body kept their dashes as internal reference docs). llms.txt also got two negative-framing rewrites ("Never pile on caveats or risk language → fear" → "Be brief and confident; the user expects this to feel routine" and "State or ask, don't hedge") and the FAQ's "customers" mention is now "the teams using Gravel" so agent-perspective stays consistent. Total: 374 Python + 217 sdk-ts + all Go tests green.

- **v0.9.1** — Claude's de_platform install follow-up (2026-05-20). Same project as Olly's v0.9.0 dogfooding, ten more rough edges Claude surfaced after the v0.9.0 fix wave. **App-breaking (P0):** `create_gravel_router` no longer crashes the host FastAPI app at boot when the Postgres driver is missing. New `try_open_database` catches ImportError / ModuleNotFoundError / ValueError and degrades to engine=None with an actionable stderr message ("install psycopg2-binary"). Pre-fix, `uvicorn --reload` would crash on import and keep the last-good build serving 200s while every LLM endpoint hung — silent zombie. Pinned by `test_create_gravel_router_survives_broken_driver`. **psycopg metadata corrected:** the `[postgres]` extra declared `psycopg[binary]>=3.1` (psycopg3) but `db/__init__.py` passes a bare `postgresql://` to SQLAlchemy which dispatches to psycopg2. Internal contradiction — `pip install 'artanis-gravel[postgres]'` got psycopg3 and the runtime still crashed looking for psycopg2. Flipped to `psycopg2-binary>=2.9` to match the v0.9.0 wizard's NeedsPsycopg2 auto-add. **Trace correctness:** `anthropic.messages.parse()` is now patched alongside `.create()` — pre-fix, structured-output calls only landed as a generic `fetch:anthropic.messages` row with no model / input richness. Both methods share the same fetch-tracing-disabled suppression so a single `.create` or `.parse` call produces exactly one row. Real-SDK behavioural tests (`test_tracing_anthropic_real.py`) exercise the actual `anthropic.Anthropic()` against an `httpx.MockTransport` — no fake-class drift, per the audit-seams-not-parts memory. **gravel_config emits `# noqa: E402`** on the SDK import so projects with ruff in their pre-commit don't get a lint failure on every fresh install (the env-loader-before-SDK-import shape is intentional). **Pre-commit hook is venv-aware:** `uv run gravel manifest --check` for uv projects, `pnpm exec gravel manifest --check` for pnpm, `npx -y gravel manifest --check` for npm, etc. Pre-fix the bare `gravel` command wasn't on PATH unless the user activated the venv. **Pre-commit YAML indent detection:** when the existing `.pre-commit-config.yaml` uses 4-space indent, the inserted block is re-indented to match — pre-fix a 2-space block in a 4-space file produced invalid YAML. **`gravel traces --apply` patches in place:** new `PatchConfigForDatabase` surgically inserts the database block into the user's existing `gravel_config.{py,ts}` instead of regenerating from scratch. Hand-edits (# noqa pragmas, custom getUser bodies, scan-roots fallback config) survive. **Polyglot detection:** hybrid Next.js + FastAPI repos detect as Python-primary AND get the next.config skipTrailingSlashRedirect / Clerk middleware publicRoutes / vercel.json rewrite patches the mount pillar previously gated behind language=ts. New `Detection.PolyglotNextFramework` + `Detection.PolyglotAuth` fields carry the secondary-language signals; `planMountHostWiring` consults both. Result: dashboard now works on Vercel deploys for hybrid repos, not just localhost via FastAPI direct.

- **v0.9.0** — Olly's de_platform dogfooding (2026-05-20) surfaced eleven items. All fixed; full-repo scanner + nuclear-escape editor + opt-in hook make this a minor bump. **Cross-stack PR-body parity (critical fix):** v0.8.0's commit message claimed "PR body now explains the .gravel/manifest.json change for all six observable cases" — TRUE in `packages/sdk-ts/src/github/create-pr.ts`, FALSE in `python/gravel/src/artanis_gravel/_github_api.py`. Olly's de_platform install (FastAPI/Python) hit the broken Python path on the latest SDK and shipped PR #249 with neither manifest explainer nor feedback link. New `python/gravel/src/artanis_gravel/_pr_body.py` ports `composeBody` + `describeManifestDiff` + `defaultPRTitle` + `computeManifestDiffSummary` verbatim from TS; both stacks now pin a shared fixture at `tests/fixtures/pr-body/cases.json` ([`pr-body-cross-stack.test.ts`](https://github.com/artanis-ai/gravel/blob/main/packages/sdk-ts/tests/pr-body-cross-stack.test.ts) + [`test_pr_body_cross_stack.py`](https://github.com/artanis-ai/gravel/blob/main/python/gravel/tests/test_pr_body_cross_stack.py)) — drift on either side now fails the test on both. Third documented case of the same audit-seams-not-parts pattern after v0.5.22 zero-trace and v0.7.0 Gemini own-property arrows. **Step 0 (version check) in llms.txt:** the agent MUST run `gravel doctor --json` first and surface the upgrade. `gravel doctor --json` now emits `installHint` — the exact `uv add 'artanis-gravel>=N' --upgrade-package artanis-gravel` / `pnpm add @artanis-ai/gravel@latest` / etc. command per detected stack. Olly's session burned an extended stretch on `artanis-gravel==0.5.5` because no step ever checked. **Scanner overhaul:** dropped the hardcoded `prompts/templates/assistants/agents/prompt/` allowlist + the `prompt_scan_roots` config band-aid; the v0.9.0 scanner walks the full repo via `git ls-files --cached --others --exclude-standard` (respecting .gitignore), falls back to FS walk + ignore list (`node_modules`, `.venv`, `dist`, `.next`, etc.) when not a git checkout. Extensions widened to `.md` / `.markdown` / `.txt` / `.mdx` / `.mdc` (Cursor rules + MDX). Existing `promptScanRoots` config emits a one-line deprecation warning. Ported in lockstep across `cli/internal/manifest/scan.go`, `packages/sdk-ts/src/manifest/scan.ts`, `python/gravel/src/artanis_gravel/manifest/scan.py`. **Editor zero-escape:** stripped ALL backslash-escaped CommonMark punctuation (square brackets, parens, exclamation, etc.) + decode HTML entities the serializer emits + preserve trailing newlines. Literal `\\` (escaped backslash) is protected via NUL placeholder so `\\n` (escaped-backslash-then-n) round-trips correctly. 13 round-trip integration tests against a real Tiptap editor exercise every Olly bug class. **Pre-commit hook opt-in:** replaced `--no-hook` with `--with-hook` (default off). Olly's complaint was the hook installed silently; now the agent must ask. **Eliminated spurious `gravel.db`:** the Python wizard no longer writes a stub `file:.gravel/dev.db` URL for prompts-only installs — emits empty `database={'url': ''}` instead. Fresh `gravel mount --apply` on a Python project now creates zero SQLite files. **psycopg2 auto-detect:** the traces pillar now detects `DATABASE_URL=postgresql://` on Python projects and adds `psycopg2-binary` to pyproject.toml as part of `--apply` (was Olly's #10 from the v0.6.2 dogfooding; bites again on every hybrid Next.js + FastAPI install). **llms.txt narration overhaul:** mandates TodoWrite enumeration up front, one-sentence-per-action narration with no hedging language, explicit pause-between-pillars with user verification, password-explainer paragraph the agent reads when the topic comes up. Total: Python+TS+Go test suites green; 407 dashboard / 374 Python / 213 sdk-ts / all Go.

- **v0.8.4** — Dashboard polish from continued landlord-ai dogfooding. **Submission title prefill:** the SubmitModal title field now auto-fills with `Update <basename>` / `Update X and Y` / `Update X, Y and Z` (no Oxford comma, verbatim from Yousef's dogfooding spec) on open so reviewers don't see an empty field; reviewers can still edit before sending. **Persistent Saved indicator:** the editor toolbar's "Saved" no longer linger-then-vanishes after 1.5s — it stays visible at rest, only flipping to a "Saving" spinner the moment unsaved input is detected (previously the indicator flashed during the sync localStorage write and was invisible to the eye). The "Saved · not yet submitted" header badge is gone; the inline indicator now carries the at-rest signal alone. Submit button dropped its trailing arrow. **Env dropped from SampleReviewDialog:** the v0.8.0 wave removed Env from the Samples *table* but left it in the detail Stats — same domain-expert-confusion lesson applies in the dialog. MetadataStrip is now 5-cell. **Hero mockup refresh on `artanis.ai/gravel`** (this rev's landing page tracks v0.8.0 → v0.8.4): mockup matches the actual rendered dashboard 1:1 (real SampleReviewDialog modal shape, real MarkdownToolbar in PromptDetail); "your-app.com" gets a shiny shimmer + an HTTPS lock; OG image lock + window dots scaled up; all prompt cards + all sample rows are clickable; click-hint GIF auto-shows/dismisses.

- **v0.8.0** — Agent-friendly install flow + a sweep of dogfooding-feedback fixes (Yousef on de_platform + Olly's v0.6.2 friction list). The big shape change: the wizard no longer assumes a TTY. **Three new pillar subcommands** — `gravel mount`, `gravel prompts`, `gravel traces` — each with `--plan` (emits a stable JSON `actions[]` document) and `--apply` (does the work). `gravel detect --json` reports the host stack (language, framework, package manager, DB, auth, LLM SDKs) so agents can narrate before any pillar runs. `gravel init` stays as the interactive human flow. `home-page/gravel/llms.txt` rewritten end-to-end around this new contract: tell the agent to walk pillar-by-pillar, ask user consent at each step. **Host-framework auto-wiring** lives in `cli/internal/wizard/host_wiring.go` — Next.js `skipTrailingSlashRedirect`, Clerk middleware publicRoutes patch, vercel.json rewrite insertion, FastAPI router include — all idempotent so re-runs don't compound. **Silent data-loss fix:** `GravelTimestamp.process_bind_param` now converts `int(ms)` → `tz-aware datetime` on Postgres so TIMESTAMPTZ columns accept the persister's writes (Olly #19 — v0.6.2 lost every Python + Postgres trace silently). **FastAPI 307 host-leak closed:** the gravel router now creates with `redirect_slashes=False` so a missing trailing slash doesn't leak `http://127.0.0.1:<port>/...` to the upstream proxy (Olly #14). **WYSIWYG editor round-trip preservation:** tiptap-markdown's conservative backslash escapes on `-` / `_` / `*` / `#` are now stripped on serialise, and `breaks: true` preserves single newlines — Yousef's PR #247 on de_platform was the canonical regression (unedited prompts showing +5/-2 diff stats from the editor alone). **Dashboard sweep:** Submit-success modal replaces the inline-green strip; persistent "Saved · not yet submitted" badge plus a Submit→ button in PromptDetail that pulse-highlights the bulk-submit button on /prompts (CSS keyframe, respects prefers-reduced-motion); Samples table drops the "env" column (domain experts don't care); review-pane feedback no longer auto-advances after submit, shows a "✓ Feedback recorded" pill, and the Skip button relabels to "Next →" so users control when to move on; PDF attachments render inline in the input panel by default (no more tiny button) and "open in new tab" converts data: URIs to blob URLs (about:blank fix); dashboard hint commands now emit `uvx --from artanis-gravel gravel ...` (the bare `uvx artanis-gravel` form didn't auto-resolve because the package name differs from the bin name — Olly's first install bite). **PR shape:** default title is now software-default `Update <basename>, <basename> and N others`; PR body explains the `.gravel/manifest.json` change for all six observable cases (first_add, added, edited, removed, renamed, anchors_changed); footer links to `gravel.artanis.ai/feedback?repo=&branch=` which bounces immediately to `mailto:yousef@artanis.ai` with repo/branch context prefilled. **GitHub App install:** the "we couldn't tell which install this is" error page now explains the non-org-owner case (the most common reason GitHub sends a callback without `installation_id`), and `setup_action=request` gets its own "your request has been sent" page instead of falling into the generic error path. **Landing:** OG image tagline is now "Prompt management for vertical agents"; `your-app.com` bumped to 40px with an underline tint; hero gets a subtle CSS scale-pulse on the same string (prefers-reduced-motion respected).

- **v0.7.1** — Three real shipping bugs in v0.7.0's Gemini support, surfaced by grounding every test against a live `gemini-flash-latest` capture (2026-05-19). (a) **TS auto-patch was a no-op against the real SDK.** `@google/genai` v1 assigns `generateContent` / `generateContentStream` as own-property arrow functions on each `Models` instance (delegating to `*Internal` prototype methods); `Models.prototype.generateContent` is `undefined`. The v0.7.0 patch checked that path, found nothing, silently skipped — every customer call went untraced. Patch rewired to wrap the prototype-level `*Internal` methods (which the public methods invoke). New `tracing-gemini-integration.test.ts` exercises the real `@google/genai` against a local HTTP mock so the bug class can't return. Python equivalent verified clean: `google.genai.models.Models.generate_content` is genuinely class-level. (b) **Token strip never appeared on Gemini fixtures.** `ReviewSurface.extractUsage` only recognised `output.usage` (OpenAI / Anthropic / Vercel-AI) and `output.llm_output.token_usage` (LangChain). Gemini's `usage_metadata` / `usageMetadata` fell through to null. Added both casings. (c) **Reasoning tokens were silently dropped from the UI.** Gemini "thoughts" (e.g. 58 of 73 tokens on a one-word reply) and OpenAI o-series "reasoning_tokens" weren't in `tokensFromUsage` — customers pay for these but the dashboard never showed them. New `reasoning` field on the return shape; new `THINKING` pill in `TokenUsageStrip` renders when > 0. Plus: all 9 Gemini fixtures re-grounded in real captures including the v1beta-required `thought_signature` on every part (also enforced server-side: echoing a `function_call` without it triggers INVALID_ARGUMENT). New stack fixture `ts-hono-pnpm-noauth-gemini` mirrors the Anthropic / OpenAI shape in `gravel-test-fixtures`. Tracing journey for Gemini in `gravel-test-fixtures/journeys/tracing/gemini.mjs` (full SQLite roundtrip via real `@google/genai` SDK against a Gemini-aware mock LLM server). Mock LLM server's response shape now mirrors what production Gemini returned today — including `thoughtsTokenCount`, `serviceTier`, `promptTokensDetails`, `responseId`, `thoughtSignature` — so the journey assertion proves something about real-world behaviour, not a fictional contract.

- **v0.7.0** — Gemini (Google) joins OpenAI / Anthropic as a first-class provider. Python tracer in `python/gravel/src/artanis_gravel/tracing/gemini_patch.py` monkey-patches `google.genai.models.Models.generate_content` and `.generate_content_stream` at the class level; sync + streaming covered, async (the `.aio.models.*` namespace) parked on the roadmap alongside async OpenAI / Anthropic. TS tracer in `packages/sdk-ts/src/tracing/gemini.ts` patches `@google/genai`'s `Models.prototype.generateContent` / `.generateContentStream` via the existing idempotence-guarded prototype-walk pattern. Both stacks trace under the same canonical name `gemini.models.generate_content` (matching the OpenAI / Anthropic snake_case convention). Same suppression flags apply unchanged: `sdk_tracing_disabled` (when LangChain owns the trace) and `fetch_tracing_disabled` (so the underlying httpx call doesn't double-record). Dashboard ships a dedicated `GeminiChat` renderer covering: `system_instruction` (config-level, not in the conversation), the full Part taxonomy (`text`, `inline_data` multimodal, `file_data`, `function_call`, `function_response`), `finish_reason` non-`STOP` captions, `safety_ratings[]` disclosure with the non-`NEGLIGIBLE` breakdown, and structured-output `response_mime_type: 'application/json'` JSON parsing. Snake_case (Python) and camelCase (TS) both accepted via `pickField`. `tokensFromUsage` extended with `prompt_token_count` / `promptTokenCount` / `candidates_token_count` / `candidatesTokenCount` / `total_token_count` / `totalTokenCount` so the existing TokenUsageStrip just works. 9 fixtures (parity with Anthropic), 9 Python pytest cases, 5 TS vitest cases. Wizard `LLMGemini` detection added for both `google-genai` (Python) and `@google/genai` (TS) — the install summary now lists Gemini alongside OpenAI / Anthropic when present. Vertex AI works transparently (same SDK).

- **v0.6.2** — `gravel init` now aborts when `uv add` / `pnpm add` fails. v0.6.1 (and earlier) printed `Install failed; re-run yourself: <cmd>` and then fell through to Step 1 (Dashboard), writing config files that referenced an SDK the project hadn't actually installed. The wizard now: (a) prints the captured stderr so the user can see *why* the install failed (PyPI propagation lag, dependency conflict, etc.), and (b) returns a non-zero error so cobra exits before any pillar runs. Power users can still pass the hidden `--skip-sdk-install` flag if they want to add the dep themselves.

- **v0.6.1** — Python-repr fallback in the Review surface. LangChain's Python `on_tool_start` callback hands us `input_str = str(kwargs_dict)` — a Python repr (single quotes, capital `True/False/None`), not JSON. The v0.6.0 renderer's `JSON.parse` quietly failed on this and fell back to dumping the raw repr string into the dashboard. Two-pronged fix: (1) New `tryParseStructuredString(s)` in `packages/dashboard/src/lib/parseStructured.ts` walks Python-repr strings, rewriting single-quoted literals + `True/False/None` to JSON, with quote-respecting state-machine so values with apostrophes don't corrupt; LangchainTool + LangchainChatModel now route through it. 11 pinning unit tests + a behavioural test in `LangchainTool.test.tsx` against the exact screenshot payload. (2) Python `on_tool_start` in `langchain_patch.py` now also captures LC's structured `inputs: dict` kwarg (when present) under `input["input"]`, so newly-traced rows ship a dict instead of relying on the dashboard to parse the repr. Renderer prefers the dict over `input_str`.

- **v0.6.0** — Review-surface rewrite. Every persisted trace shape now has a dedicated renderer; the old `extractMessages` / `extractOutput` heuristics plus the `JsonTree` fallback are deleted. New `detectSource(name, input, output)` dispatches to one of 12 `SourceKind`s — `openai-chat`, `openai-responses`, `openai-embeddings`, `anthropic-messages`, `vercel-ai-text`, `vercel-ai-object`, `langchain-llm`, `langchain-chat-model`, `langchain-chain`, `langchain-tool`, `langchain-retriever`, plus `unknown` (which delegates to the recursive `HumanValue` and **never** JSON-dumps). Pinned by 41 fixture files at `packages/dashboard/tests/fixtures/sources/`. Cross-cutting chrome: `FetchHeader` (URL + method + status for fetch-captured samples), `ErrorBanner` (red panel for `metadata.error`), `TokenUsageStrip` (works across OpenAI Chat / Responses / Anthropic / Vercel AI v4+ naming), `StreamObservations` (chunk count + chunks/sec throughput + collapsible chunk list for streamed calls), `TraceNavigator` (multi-step trace step strip at the top of the dialog; clicking a sibling refetches it inline), `MetadataStrip` (separates user-supplied keys from SDK-emitted ones). Collapse defaults unified: system / developer / non-last user collapsed, the LAST user message + assistant + tool / function open. JSON-encoded `response_format: json_object` / `json_schema` content auto-parses; `additional_kwargs.parsed` (LangChain structured output) renders the parsed value instead of duplicating it. SDK-side: new `sdkTracingDisabled` AsyncLocalStorage flag (TS) / contextvar (Python) suppresses OpenAI / Anthropic SDK patches while LangChain owns the trace, killing the `langchain.chat_model` + `openai.chat.completions.create` duplicate-row class. Vercel AI tracer rewritten for v4+ field names (`inputTokens` / `outputTokens` / `inputSchema`); v3 back-compat dropped. LangChain callback handler captures tool invocations (`langchain.tool.<id>`) and retriever runs (`langchain.retriever.<id>`) as standalone trace rows, not just state observations. Image + PDF attachments are clickable into a lightbox dialog (with a nested-dialog Escape stack so the lightbox closes without dismissing the underlying review dialog). 301 dashboard tests, 187 sdk-ts tests, 321 Python tests — all green.

- **v0.5.7** — `GET /api/prompts/{id}` added to the Python SDK; PromptDetail page worked for the first time on Python hosts.
- **v0.5.8** — Real PyPI lookup in `/api/version`; UpdateBanner stopped reporting `hasUpdate: false` unconditionally. `CURRENT_VERSION = "0.1.0"` hardcoded value retired in favour of `importlib.metadata`.
- **v0.5.9** — Major Python SDK rewire: every route now flows through `_handler.py`. Killed `asgi.py` + `django.py` placeholders; added `/api/migrations/status`, `POST /api/prompts/submit`, `/api/github/install` + `/callback`, `POST /api/auth/view-as`; fixed form-encoded login redirect; added per-IP login rate-limiting; `/_assets/<file>` content-type map; `/api/prompts` honours `GRAVEL_REPO_ROOT` via the typed `manifest/io` helpers.
- **v0.5.10** — Heavy cross-stack journey coverage. New `tests/test_embedded_journey.py` pins byte-exact char-offset slicing across FastAPI/ASGI/WSGI/Django/Flask and PR + manifest rewrite invariants for adding/removing lines, multi-prompt edits in one file, hash-update semantics.
- **v0.5.11** — Closes the residual audit findings: TS auth gates on `/api/prompts`, `/api/samples`, `/api/github/status` to match Python (information disclosure fix). Unit tests for `_github_api.py` (24) and `samples_query.py` (50). Bare-`except` hygiene in `auth.py` / `_github_api.py` / `judge/client.py`. Session TTL constant decoupled from the magic-number cookie Max-Age.
- **v0.5.12** — Generic-fetch tracing port to Python (httpx sync + async, requests, aiohttp, stdlib urllib; 41 tests against an in-process `http.server` thread). TS `routes.ts` split into per-domain files (`auth.ts`, `version.ts`, `migrations.ts`, `prompts.ts`, `github.ts`, `samples.ts`, `shell.ts`, `assets.ts`) to match the Python `_handler.py` shape; cookies + shell helpers extracted. New Playwright E2E suite for the dashboard (15 tests covering auth + samples + prompts + github flows), wired into CI as a dedicated `dashboard-e2e` job.
- **v0.5.13** — Wizard detects a dev server already listening on the framework default port and warns "restart your server" so newly-written mount code is actually picked up (real customer 404 closed). Dashboard component test coverage roughly doubled with new PayloadShape (12), CopyableCode (6), and Login (8) suites. CI caches Playwright browser binaries by version, saving ~30s per run. `gravel-test-fixtures` gains a `manifest:deep-scan` journey that runs `gravel scan --deep --yes` and asserts the manifest gains embedded entries in declared `deepScanFiles` paths.
- **v0.5.14** — Deep-scan rewrite. (1) Killed the dead OpenAI file-by-file scan path (`packages/sdk-ts/src/manifest/deep-scan.ts`) — only its test imported it, never wired into the wizard. One deep-scan now: the agent-delegated one. (2) Agent contract switched from line-only + `snippet` to `startsWith` / `endsWith` anchors — short substrings of the prompt content as they literally appear on `lineStart` / `lineEnd`. The SDK resolves the anchors to precise offsets itself (str.find on the bounded line); no more "the slice includes the surrounding `const X = "..."` syntax." (3) Manifest `charStart` / `charEnd` are now Unicode code points everywhere. Previously: Go wrote UTF-8 byte offsets, TS wrote UTF-16 code units, Python sliced as code points — any non-ASCII content (em-dash, smart quote, accented letter, emoji) desynced the three counts and the handler cut the wrong characters. New helpers in `manifest/offsets.{go,ts}` (`SliceByCodePoints`, `CodePointLen`, `LineContentCodePoints`); all readers/writers go through them. Round-trip multi-byte tests added in Go, TS, Python.
- **v0.5.15** — Dashboard UX bug fixes. (a) Modal backdrop now covers the full viewport — switched to `createPortal(document.body)` so `fixed inset-0` is viewport-anchored regardless of any transform / backdrop-blur ancestor. The earlier "no portal because of host CSS scope" comment was wrong; Tailwind classes work fine through portals. (b) CLI commands shown in the dashboard (`gravel migrate`, `gravel manifest --update`, `gravel init --traces`) now use the SDK-runtime-aware form: `npx @artanis-ai/gravel ...` for TS hosts, `uvx artanis-gravel ...` for Python hosts. Earlier copy assumed a globally-installed `gravel` binary, which the wizard doesn't set up for users who invoked the wizard via `uvx` / `npx`. New `window.__GRAVEL_RUNTIME__` global injected by the shell rewriter; new `gravelCommand()` helper in `packages/dashboard/src/lib/runtime.ts`. (c) Dropped the fictional `gravel github connect` command from the GithubNotConnectedDialog — there's no such subcommand; the in-dashboard "Install GitHub App" button is the real install flow and the dialog already explains it.
- **v0.5.16** — Wizard tab-completion rendering bug. The path-completion candidate list (double-Tab in the manual-prompt-entry flow) printed each filename followed by a bare `\n`. The terminal was in raw mode at that point, where `\n` only moves the cursor down without returning to column 0 — so each candidate landed at the column where the previous one ended, producing a visible staircase indentation. Fixed `printCandidates` in `cli/internal/wizard/path_completion.go` to emit `\r\n`. Pinned with a unit test that asserts the exact bytes.
- **v0.5.17** — Multi-repo gravel-bot installs + human error pages everywhere in the install flow. (a) Lifted the single-repo restriction: one GitHub App install can now cover N repos in an org. CP schema: `gh_installations` PK is now composite `(installation_id, repo_owner, repo_name)`; install callback upserts one row per repo; mint endpoint accepts `repo_full_name` and verifies the (installation, repo) pair exists before issuing the token. SDKs (TS + Python) pass their `repo_full_name` from .env.local on every mint. The wizard's "Install GitHub App" link now includes an `expected_repo=owner/name` hint (detected via `git remote get-url origin`) so the callback picks the right repo when the install covers multiple — no more "the SDK wrote the wrong repo to my .env.local" footgun. (b) Every browser-visible CP route in the install/auth chain (install/start, install/callback, both with ~10 distinct failure paths between them) now returns a styled HTML error page with title + body + recovery CTA, never raw `NextResponse.json({error})`. Helper lifted to `lib/error-page.ts`. New SDK helpers: `repo-detect.{ts,py}` with `parseGithubRemoteUrl` covering SSH/HTTPS/git:// + auth tokens; 17 parser tests in each stack.
- **v0.5.26** — No more raw JSON dumps in the Review surface. The dashboard had three remaining `<pre>{JSON.stringify(value, null, 2)}</pre>` fallback paths (deep nesting, dense objects, the `extractMessages/extractOutput` "unknown" block type) — exactly the wall-of-quoted-string output Yousef saw on tool-result payloads. New `JsonTree` component, a trimmed port of `platform/src/components/dashboard/renderers/object-renderer.tsx`: collapsible at every object + array node, small flat objects render inline as `key: value · key: value`, primitives get a tint per type (string=forest, number=primary-dark, boolean=accent, null=muted-italic), long strings truncate with a show-more affordance, copy-as-JSON button at the root. Adapted to Gravel's warm/cream design tokens (the platform component uses slate + shadcn). `BlockView` case `unknown`, `HumanValue` depth/density bailouts, and the deleted `safeJson` helper all route through the new component — there is no `<pre>` left in `SampleReviewDialog.tsx`. 6 new `JsonTree.test.tsx` cases pin: small flat objects render inline; deep objects render collapsible with no `<pre>` on the page; click expands closed nodes; long strings truncate; null / boolean / empty-string get type-distinct styling.
- **v0.5.25** — Two bugs in one release. (a) **Output pane on fetch-captured samples rendered raw JSON.** `extractMessages` already unwraps the `{ status, body }` envelope the raw-fetch patch persists, but `extractOutput` didn't — so `fetch:openai.chat.completions` samples fell through to the JSON-dump path while the input side rendered cleanly. New `messages.test.ts` cases pin both providers' unwrap path. (b) **Duplicate rows for every openai/anthropic call on Python.** Each LLM call landed twice — once via the openai SDK patch and once via the fetch patch, because the SDK routes through httpx which fetch_patch intercepts. The TS canon has had `gravelContext.fetchTracingDisabled` for releases (AsyncLocalStorage flag SDK patches set around `original(...)` so fetch.ts skips its own recording); the Python side was missing the entire mechanism. Port: new `fetch_tracing_disabled` field on `_ContextState`, `run_with_fetch_tracing_disabled` and `is_fetch_tracing_disabled` helpers on the gravel context, `fetch_patch._is_disabled` reads the flag, openai_patch and anthropic_patch wrap their `original(self, *args, **kwargs)` call in the helper. End-to-end regression tests on both stacks pin the seam: `test_sdk_patch_does_not_double_record_via_fetch` runs a real httpx call through the python fetch patch with + without the suppression flag and asserts the row delta is exactly 1 vs 0; new `tracing-dedup.test.ts` loads both TS patches simultaneously, runs an openai-shape fetch inside vs outside `runWithFetchTracingDisabled`, asserts persistSample is called exactly once for the suppressed call. Langchain dedup against ChatOpenAI is still a known issue (acknowledged in `langchain_patch.py` comments — "We dedupe in the future via a Langchain-injected request id"); deferred.
- **v0.5.24** — Cross-stack response-shape parity for `/api/samples/:id`. The dashboard's `SampleReviewDialog` destructures `const { sample, feedback } = data` and reads `sample.input` — but the Python handler was returning a flat object (no `sample` wrapper, `feedback[].timestamp` instead of `created_at`, no `related` array, missing `tokens_in/out`/`feedback_count`/`feedback_score` on the sample). Result: click-on-a-trace crashed with `Cannot read properties of undefined (reading 'input')` on every Python-backed install. Fix: Python `get_sample_detail` restructured to match `packages/sdk-ts/src/samples/query.ts:SampleDetail` byte-for-byte — `{ sample: SampleListItem & { commit_sha, input, output, metadata }, feedback: FeedbackItem[], related: SampleListItem[] }`. New `_summarize_row` helper shared between `list_samples` and `get_sample_detail` so the summary fields can't drift again. Feedback ordering switched to `created_at` DESC (TS canon). Three new regression tests pin the response shape end-to-end: `test_api_samples_detail_shape_matches_dashboard_contract` stands up the full FastAPI → dispatcher → samples_query chain, hits /api/samples/:id, and asserts every field the dashboard's `SampleDetailResponse` interface declares; plus the missing-id 404 path. The Playwright E2E samples spec runs against a prompts-only install (no DATABASE_URL) so it never had rows to click on — extending it to seed a DB and exercise SampleReviewDialog is the next follow-up.
- **v0.5.23** — Read-side companion to v0.5.22: the dashboard's /api/samples 500'd for every Python SQLite customer because of cross-stack schema drift. The Go SDK's `cli/internal/migrate/sql/sqlite_bootstrap.sql` declares `timestamp / started_at / completed_at / created_at` as `INTEGER NOT NULL DEFAULT (unixepoch() * 1000)` — int unix ms, matching the TS Drizzle SQLite schema. The Python SQLAlchemy schema declared the same columns as `DateTime(timezone=True)`. SQLite's loose typing accepted the persister's datetime-as-ISO-text writes into the INTEGER column, but `created_at`'s server-default `(unixepoch() * 1000)` produced a real int. When the dashboard called `list_samples`, SQLAlchemy's DateTime result-processor crashed: `fromisoformat: argument must be str` on the int rows. Fix: new `GravelTimestamp` TypeDecorator in `schema.py` — BIGINT on SQLite (matches Go bootstrap), TIMESTAMPTZ on Postgres (matches Go bootstrap); bind converts datetime → int(ms) for SQLite; result accepts int / str (legacy pre-fix rows) / datetime and always returns timezone-aware datetime. Filter params like `from_="2026-04-30"` parse to int(ms) too. New regression tests pin the seam the v0.5.22 release missed: write via persister → read via `list_samples` (the actual /api/samples handler) → assert ISO string output; plus a test that re-creates the Go bootstrap's DDL verbatim and proves `list_samples` survives both legacy text rows and current int rows. The schema-drift CI was blind to this because `dump.ts` only inspects `postgres.ts`, not `sqlite.ts` or the Go bootstrap SQL — extending the dump to compare SQLite-vs-SQLite + Python-vs-Go-bootstrap is the next follow-up.
- **v0.5.22** — The audit-uncovered bug we've been hunting: silent zero-trace failure on every Python host AND every non-Next.js TS host. The wizard's traces pillar created the gravel_* tables and printed "auto-tracing wired up", but on the actual code path neither half of the runtime ever installed: Python's `create_gravel_router` / `_build_context` / `_build_proto_context` opened the engine but never imported `artanis_gravel.auto` and never called `set_gravel_tracing_config(...)`; TS's `createGravelHandler` plumbed the config to the persister but Express / Fastify / Hono / generic-Node hosts had nothing import `@artanis-ai/gravel/auto` to install the LLM-SDK patches (only Next.js was wired, via instrumentation.ts). New `install_auto_tracing(engine)` helper in `tracing/__init__.py` installs the openai / anthropic / langchain / fetch patches via their explicit entrypoints (not bare `import` — idempotent across test resets) and points the persister at the engine. Called from all three Python framework adapters. TS `createGravelHandler` mirrors the fix with `void import('../auto.js')` at first-call time. Both stacks get end-to-end regression coverage that fails loudly if the seam regresses: real FastAPI router + sqlite + requests.post to an OpenAI-shape URL → assert one row in gravel_samples; real createGravelHandler + mocked persistSample + fetch to an openai URL → assert exactly one captured sample. The audit missed this because it was a code-review pass that checked the parts in isolation but not the seams; the related canary (fixture-results.json covers only 4/18 fixtures) was tagged P2 instead of P0.
- **v0.5.21** — (a) Push-status pre-flight on the Prompts surface. A prompt whose file isn't on the upstream branch can't be sent for review yet — GitHub returns 404 when the SDK tries to read it. `/api/prompts` now enriches each row with `pushed: boolean` by running one `git ls-tree @{u} -- <paths>` against the manifest's paths; the dashboard renders an amber "Unpushed" badge on those cards and the SubmitModal blocks submit with a clear Alert (instead of users discovering it through a `github_failed: Not Found` error after they hit send). Server-side `submit_drafts` (Python + TS) adds a matching pre-flight that throws `prompt_not_pushed` as defence-in-depth. New helpers: `_push_status.py` + `manifest/push-status.ts`. (b) Phantom-diff fix in PromptDetail editor. The SuggestionDiff overlay was showing strikethroughs/insertions on unedited prompts because the "original" side was processed by a regex approximation (`markdownToPlainText`) while the "current" side ran through tiptap-markdown's CommonMark parser — the two disagreed on intraword underscores (`needs_human` → `needshuman` per regex, but `needs_human` per parser) and a few other syntax edges. New `markdownToDocText` spins up a transient headless tiptap editor with the same extension set and reads its `textBetween`, guaranteeing both sides of the diff go through the same parser.
- **v0.5.20** — UpdateBanner uv upgrade command was wrong twice in a row. `uv add <pkg>@<version>` (v0.5.18's "fix") uses npm-style `@version` syntax that uv treats as `name @ path` — uv tries to resolve the version string as a local filesystem path and errors out. Settled on `uv sync --upgrade-package <pkg>`, which respects the existing `>=…` pin the wizard writes in pyproject.toml + uv.lock and lifts to the latest compatible version. Also switched poetry to the equivalent `poetry update <pkg>` for consistency. Test pinned to the new exact string.
- **v0.5.19** — Dashboard button feedback + error surfacing. (a) `api.ts` rewrite: non-2xx responses now throw a new `ApiError` class carrying `status` / `code` / `serverMessage` / `details`. Previously only the `error` code was surfaced; the actual `message` and upstream `details` (e.g. GitHub's "Not Found") were thrown away — so a `github_failed` looked like literally the string "github_failed" with no clue what went wrong. (b) New `<Alert>` component: replaces the "one line of red text below the submit button" pattern with a proper alert block carrying humanised title + server message + folded `<details>` disclosure for raw upstream output. (c) New `<Spinner>` component (inline, `currentColor`-tinted). (d) SubmitModal: errors now render as Alert at the top of the form with the full server context; submit button shows the spinner while in flight; the existing "Send for review" copy stays. (e) "Install GitHub App" button: `aria-busy` + inline spinner + obviously-darker disabled state (was barely distinguishable from active); busy state extends through `isSuccess` so the disabled state covers the navigate-to-GitHub window. Three new SubmitModal tests pin: ApiError shows up as a full Alert with message + details, unmapped codes fall back to a generic title, spinner appears inside the in-flight submit button. 92 → 95 tests.
- **v0.5.18** — Dashboard copy + upgrade-command fixes. (a) Stripped "PR" / "pull request" / "repo" from non-engineer-facing copy across the Prompts surface. Submit button: "Open PR" → "Send for review". Modal subtitle: "drafts will be submitted as one PR to your connected repo" → "drafts will be sent to your team together for review". Field label: "PR title" → "Title". Success toast: "PR opened" → "Your changes are in for review". Validation error rewritten so it doesn't mention PRs. GithubNotConnectedDialog rewritten to say "Gravel isn't connected to your team's codebase" instead of "GitHub App isn't installed on the repo". Engineer-facing copy (DeveloperNote blocks, the "For the developer" subsection of the GH dialog, code comments) keeps the technical terms. (b) Update banner's upgrade-command for `uv` switched from `uv pip install --upgrade <pkg>==<version>` to `uv add <pkg>@<version>`. The wizard installs into a project-managed uv env (pyproject.toml + uv.lock); `uv pip install` modifies the venv but bypasses both files, so the next `uv sync` silently reverts the upgrade. (c) Update banner now appends "then restart your server" next to the copy command — uvicorn / next-dev keep the old wheel loaded in memory until the process restarts; not saying so was sending users into a "I upgraded but the banner is still there / submit still 502s" loop.

## What ships in `artanis-ai/gravel` today

### TypeScript SDK (`@artanis-ai/gravel`)
- Schemas (Postgres + SQLite), DB connector, idempotent bootstrap.
- HMAC sessions, view-as, per-IP login rate-limiting.
- Dashboard routes (per-domain modules under `src/handler/routes/`): auth (login/logout/me/view-as), version + update banner, migrations status, samples (list / detail / feedback), prompts (list / detail / submit), GitHub status + install start + callback, bundled SPA + assets.
- Tracing auto-patches: OpenAI, Anthropic, LangChain, Vercel AI, generic `globalThis.fetch`. Lazy import; missing SDKs no-op. Streaming via `Symbol.asyncIterator` tee.
- Manifest tooling (fast scan + pre-commit hook installer).
- Wizard via the Go CLI: framework detection, AST-aware mount writers, config gen, `.env` writer, idempotent re-runs, deep scan, running-server detection + restart warning.
- Judge client, eval runner with bounded concurrency, Mallet `analyzePrompt`.
- **Tests:** vitest, 170 passing + 1 skipped.

### Python SDK (`artanis-gravel`)
- Full SQLAlchemy parity with TS schema.
- Same dashboard routes via the shared `_handler.py` dispatcher; FastAPI, raw ASGI, raw WSGI, Django, Flask integrations are all adapters around it.
- Tracing auto-patches: OpenAI, Anthropic, LangChain. Generic-fetch patches for httpx (sync + async), requests, aiohttp, and stdlib urllib (v0.5.12+; lazy per-transport, missing library no-ops).
- Wizard installs via the Go CLI binary (same as TS); Flask wizard auto-adds the `[flask]` extra for the a2wsgi bridge.
- Judge client, eval runner, analyze client.
- **Tests:** pytest, 291 passing + 1 skipped.

### Dashboard (Vite + React 19 + Tailwind)
- Routes shipped: Login, Samples (list + detail + feedback), Prompts (list + detail editor with embedded slice editing + draft autosave to localStorage + submit modal).
- Routes scaffolded (placeholder empty states; not in product scope yet): Datasets, Evals, Analysis. Adding a backend route table for either pillar is net-new product work and explicitly out of scope through v0.5.x.
- Components: PendingMigrationsBanner, UpdateBanner, CopyableCode, PayloadShape (provider-aware payload renderer), SubmitModal, SuggestionEditor, DiffView, GithubNotConnectedDialog.
- Bundle: ~85 KB gzipped.
- Bundled into both SDKs at release time (`tools/sync-dashboard-dist.sh` for Python; per-SDK build for TS).
- **Tests:** vitest 85 passing across 12 files; Playwright E2E 15 passing across 4 specs (auth / samples / prompts / github), driven against the live Vite dev server which mounts the real SDK handler in-process.

### Go CLI / wizard
- Framework detection across 9 stacks. Two-phase entry search; column-zero-only AST-style mount writers per language.
- Heavy coverage in `cli/internal/wizard/*_test.go`.
- Manual prompt picker with `$EDITOR` integration for humans, line-number entry for agents; tab-completion on path input.

### Examples
- Next.js (App Router + Pages), Express, Hono, Fastify, FastAPI, Django, Flask.

### CI
- `ci.yml` (TS + Python with all extras including `flask`), `schema-drift.yml` (TS vs Python schema), `publish-{npm,python}.yml` (PyPI + npm publish on tag), `version-sync.yml` (lockstep enforcement).

## `artanis-ai/gravel-cloud` (private)

- Control plane on `gravel.artanis.ai`. Auto-deploys on push to main.
- Routes listed above.
- Hardening headers (CSP, HSTS preload, X-Frame-Options DENY, etc.).
- Vitest suite covering authenticateRequest, svix webhook rejection, rate-limit, Polar webhook.
- Judge service runs as a Cloudflare Worker (`apps/judge/`).

## Cloud verification (run anytime)

```bash
curl -s https://gravel.artanis.ai/api/health
curl -s https://gravel-judge.artanis-ai.workers.dev/health
```

For an authed call against `/api/judge` or `/api/analyze`, run `gravel doctor` from a wizard-installed project — it prints a working curl recipe with the API key + project id resolved from your `.env.local`.

## What's next

Open at the time of writing:

1. Datasets + Evals pillars — these are scaffolded as empty dashboard routes; the backend route table is net-new product surface (new SDK helpers, new tables, new dashboard pages) and needs a design pass before code.
2. Fixture suite end-to-end execution: the verifier-side journeys (`manifest:deep-scan`, raw-fetch tracing) are coded but the CI machine that runs `npm install` / `poetry install` across all 14 fixtures doesn't exist yet.
