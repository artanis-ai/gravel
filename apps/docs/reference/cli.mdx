---
title: CLI
description: "gravel — single binary for install, manifest, and diagnostics."
---

```bash
gravel init                Run the install wizard
gravel manifest --check    Verify manifest is in sync (used by pre-commit hook)
gravel manifest --update   Regenerate manifest from working tree
gravel manifest --list     Print human-readable summary
gravel scan --deep         Run deep LLM scan locally (NOT YET IMPLEMENTED)
gravel migrate             Apply DB migrations (uses bootstrap.ts in v0)
gravel doctor              Diagnostic: DB, manifest, hook, tracing
gravel help                Show help
```

## Init flags

| Flag                          | Effect                                        |
| ----------------------------- | --------------------------------------------- |
| `--yes`, `--non-interactive`  | Assume yes to every prompt                    |
| `--api-key <key>`             | Pre-bake project key into `.env`              |
| `--project <id>`              | Pre-bake project ID into `.env`               |
| `--mount-path <path>`         | Override default `/admin/ai`                  |
| `--prompts`, `--no-prompts`   | Toggle the prompts pillar                     |
| `--traces`, `--no-traces`     | Toggle the traces pillar                      |
| `--no-deep-scan`              | Skip deep scan (also skipped if not implemented yet) |
| `--no-test-trace`             | Skip end-to-end test trace                    |

## Python equivalent

```bash
python -m artanis_gravel init           # same flags
python -m artanis_gravel manifest check
python -m artanis_gravel migrate
```
